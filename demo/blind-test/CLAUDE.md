# Task

Send a message with subject "Hello" and body "Test message" to the inbox `demo-inbox` using the AgentMail API.

## AgentMail API

- **Base URL:** Use the `AGENTMAIL_BASE_URL` environment variable
- **Authentication:** `Authorization: Bearer $AGENTMAIL_API_KEY`
